pipeline{
 
 agent {
        label 'WIN'
       }
 
 
stages{
      
      stage('build'){
                   steps{
                     sh 'mvn clean compile'
                     
                       }
                    }

         }

        }
